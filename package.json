{
  "name": "ribas89",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --cleanDestinationDir --gc --logLevel debug --minify --noBuildLock --forceSyncStatic --destination ./docs",
    "serve": "hugo serve -M=false --destination ./preview --ignoreCache --disableFastRender --logLevel debug --forceSyncStatic --noHTTPCache --bind 0.0.0.0"
  }
}
